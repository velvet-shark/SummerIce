{
  "name": "summerice-extension",
  "version": "2.3.1",
  "description": "A browser extension to summarize articles quickly using multiple LLM providers",
  "main": "background.js",
  "scripts": {
    "build": "npm run test && npm run build:zip",
    "test": "vitest run",
    "test:models": "node ./tests/verify-models.mjs",
    "build:zip": "node ./scripts/build-zip.mjs"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "dompurify": "^3.0.5"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^2.1.0"
  },
  "keywords": [
    "chrome-extension",
    "summarization",
    "ai",
    "readability"
  ],
  "author": "SummerIce Team",
  "license": "MIT"
}
